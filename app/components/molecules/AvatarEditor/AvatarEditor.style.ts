import { StyleSheet } from "react-native";

export default StyleSheet.create({
  wrap: { position: "relative", alignItems: "center", justifyContent: "center" },
  editBtn: { position: "absolute", right: 10, bottom: 10 },
});
